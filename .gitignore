node_modules
.next
out
build
dist

.env
.env.local
.env*.local

.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel
next-env.d.ts
.env
.env.local
node_modules
.next

# build cache + local render artifacts (not for VCS)
chrome/
tmp/
tsconfig.tsbuildinfo

# session lock (harness-local)
.claude/scheduled_tasks.lock

# Figma design source (reference only — not a served asset)
public/references/geoviz-report-template.pdf
